# 🌾 DalaScan AI

**Диагностика заболеваний пшеницы по фотографии** — MVP для хакатона на Python 3.11 и Streamlit. Бесплатный локальный инференс без внешних API и ключей.

> Для настоящего режима нужны веса `model/wheat_model.pth` и локальная проверка снимков CLIP. Если весов болезней нет, включается **демонстрация интерфейса, а не диагностика**. Перед практическим использованием необходима независимая проверка качества на ваших полях.

## Хакатон, задача и пользователь

**AgriTech AI Hackathon, Кокшетау, 17–23 сентября 2026 года. Трек 3 — компьютерное зрение в агрономии, задача 3.1.** В рамках задачи реализована диагностика болезней пшеницы; вредители и сорняки не входят в текущую версию.

Пользователи — фермер и агроном хозяйства Акмолинской области. Сценарий: сфотографировать лист, получить предварительную оценку и рекомендации по дальнейшему обследованию, сохранить PDF для агронома. Приложение помогает организовать обследование; подтверждённого влияния на урожайность или снижение потерь пока нет.

Репозиторий: [okimf6/DalaAI](https://github.com/okimf6/DalaAI). Перед сдачей команда должна проверить доступ комиссии к репозиторию и всем материалам без учётной записи разработчика.

## Команда

Состав команды — 2 участника:

- **Олег Нуртай Денисович**.
- **Мунтиев Саят Арманович**.

Индивидуальные роли и распределение вклада пока не указаны; общий перечень выполненных работ приведён ниже. ИИ-ассистент участником команды не является.

## Выполнено в ходе хакатона

- Создано приложение Streamlit: загрузка и камера, обработка ошибок, проверка качества, результаты и история сессии.
- Реализованы русский и казахский интерфейсы, справочник четырёх состояний и рекомендации.
- Подготовлены реальные данные, воспроизводимое разделение и скрипт обучения MobileNetV3 Small; сохранён обученный checkpoint с метриками.
- Подключены локальная проверка содержимого CLIP и отказ от диагноза для неподходящих или сомнительных снимков.
- Добавлен PDF-отчёт с фотографией, результатом и ограничениями.
- Подготовлены автоматические тесты, Dockerfile и инструкции; загрузчик датасета переведён на Kaggle с сохранением происхождения данных.

История коммитов и исходные файлы служат материалами для проверки выполненных работ. Публикация сервиса, полевые испытания, видео и презентация не заявляются здесь как завершённые.

## Стек, заимствования и использование ИИ

Python 3.11, Streamlit, Pillow, NumPy, PyTorch, torchvision, Transformers и ReportLab; тестирование — pytest. Основные версии зафиксированы в `requirements.txt`.

**При разработке использовался OpenAI Codex**: создание и редактирование кода, тестов, документации и помощь с подготовкой данных. Команда отвечает за проверку и понимание представленного решения. Платные API и API-ключи для работы приложения не требуются; Codex не вызывается приложением при диагностике.

Использованы открытые библиотеки и предобученные модели: MobileNetV3 Small с ImageNet-весами из torchvision для дообучения и [CLIP ViT-B/32](https://huggingface.co/openai/clip-vit-base-patch32) для проверки снимка. Авторы и лицензия датасета указаны в [DATASET_LICENSE.md](data/DATASET_LICENSE.md), сведения о CLIP — в [IMAGE_GUARD.md](model/IMAGE_GUARD.md), лицензия шрифта — в `assets/fonts/LICENSE-DejaVu.txt`. Сторонние компоненты сохраняют собственные условия использования.

Синтетические фотографии в обучающий датасет не добавлялись. Случайные отражения, повороты, crop и изменение яркости/контраста применяются только как аугментации train. Синтетические изображения в автоматических тестах проверяют код и не используются для заявления точности. Демонстрационный режим также не является обученной моделью.

## Возможности

- Загрузка JPG/JPEG, PNG, WEBP и съёмка камерой браузера.
- Проверка содержимого, целостности, EXIF-ориентации, лимитов 15 МБ / 25 Мп; RGB и уменьшение до 1280 px.
- Неблокирующие рекомендации по яркости, резкости и разрешению.
- Четыре состояния: здоровая пшеница, бурая ржавчина, жёлтая ржавчина, септориоз.
- Top-3 с полосами вероятностей, справочный риск, признаки и безопасные действия.
- Пороговая интерпретация: ≥70% — вероятное состояние; ≥45% и <70% — дополнительный снимок; <45% — состояние не определено. Пороги применяются до округления.
- Пять последних анализов в памяти текущей сессии и очистка истории.
- Светлая адаптивная тема, локальная модель CPU/CUDA и кэш загрузки весов.
- Русский и казахский языки, проверка содержимого CLIP, скачивание PDF-отчёта.

## Скриншот

**Место для скриншота:** запустите приложение, загрузите свой снимок и добавьте изображение интерфейса здесь. Не используйте демонстрационный результат как доказательство качества распознавания.

## Быстрый запуск в Windows

Установите **64-битный Python 3.11** с [python.org](https://www.python.org/downloads/). При установке включите `Add python.exe to PATH`. Проверьте `python --version`, затем откройте PowerShell в папке `dalascan-ai`.

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
streamlit run app.py
```

После установки зависимостей приложение запускается **одной командой** `streamlit run app.py`. Откройте [localhost:8501](http://localhost:8501). Для остановки нажмите Ctrl+C в терминале.

Если PowerShell блокирует активацию окружения, изменение политики не обязательно. Используйте команды ниже после успешного создания `.venv`. Если папки `.venv` нет, сначала проверьте установку Python и выполните `python -m venv .venv`:

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m streamlit run app.py
```

Установка требует интернета. После установки демонстрация и инференс работают без доступа к сети. Для CUDA нужна совместимая сборка PyTorch: используйте [официальный установщик PyTorch](https://pytorch.org/get-started/locally/) для своей ОС и видеокарты, сохраняя пару версий torch 2.7.1 / torchvision 0.22.1. CUDA выбирается автоматически при `torch.cuda.is_available()`.

## Структура

```text
dalascan-ai/
├── app.py
├── README.md, requirements.txt, pytest.ini
├── Dockerfile, .dockerignore, .gitignore
├── .streamlit/config.toml
├── assets/fonts/                # Шрифт PDF и лицензия
├── data/                        # Справочники RU/KK, переводы, источник датасета
├── model/                       # Документация; локальные веса исключены из Git
├── sample_images/README.md
├── utils/
│   ├── analysis.py              # Общий сценарий анализа
│   ├── evaluation.py            # Метрики и проверка утечки
│   ├── i18n.py, image_guard.py
│   ├── image_processing.py, model_config.py
│   ├── pdf_report.py, predictor.py
│   └── recommendations.py, ui.py
├── scripts/
│   ├── prepare_dataset.py
│   ├── download_guard.py
│   └── train_model.py
└── tests/                       # UI, изображения, модели, данные, PDF и локализация
```


## Проверка снимков перед диагнозом

В настоящем режиме перед моделью болезней запускается отдельный локальный CLIP ViT-B/32.
Он сравнивает фото с фиксированными описаниями пшеницы, других растений и посторонних объектов.
Это **экспериментальная zero-shot проверка**, а не обученная и проверенная на полях система определения вида растения.
Она может принять ячмень, рис или другой похожий злак за пшеницу и может попросить переснять настоящий лист пшеницы.

- **Похож на пшеницу**: выполняется классификация заболевания.
- **Вероятно, не пшеница**: диагноз не выдаётся.
- **Не удалось подтвердить**: диагноз не выдаётся, приложение просит переснять лист.
- **Проверка недоступна или сломалась**: настоящая диагностика блокируется; скрытого обхода нет.
- **Демонстрационный режим**: проверка пропускается с явным предупреждением, результат остаётся симуляцией.

Установка на новой машине после создания и активации окружения:

```powershell
python -m pip install -r requirements.txt
python scripts/download_guard.py
python -m streamlit run app.py
```

Однократно скачивается примерно **605 МБ** бесплатных весов из `openai/clip-vit-base-patch32`.
API-ключ не нужен. Зафиксирована версия `3d74acf9a28c67741b2f4f2ea7635f0aaf6f0268`.
После загрузки инференс работает без сети, с `local_files_only=True`; снимки никуда не передаются.
Веса проверки не входят в репозиторий: на новой машине выполните команду загрузки выше.

Каталог `model/image_guard/` исключён из Git, ZIP исходников и Docker build context.
В Docker подключайте всю папку `model` через существующую команду `--mount`.
Память и время первого запуска возрастают из-за отдельной модели; обе модели прогреваются заранее.
Время пользовательского анализа теперь включает также проверку CLIP.

Пороги, источники и ограничения описаны в [model/IMAGE_GUARD.md](model/IMAGE_GUARD.md).

## PDF-отчёт обследования

После анализа раскройте «Скачать отчёт обследования», укажите поле/участок и при необходимости заметку,
затем нажмите **«Скачать PDF»**. Язык отчёта соответствует переключателю Русский / Қазақша.

Отчёт содержит фотографию именно выполненного анализа, дату с часовым поясом, время обработки,
результат проверки снимка, Top-3 и рекомендации (если диагностика состоялась), предупреждения
о качестве и ограничениях. Для отклонённых снимков отчёт прямо сообщает, что диагноз не выполнялся.
При низкой уверенности сохраняется отказ от надёжного диагноза. Демо-отчёты помечены на каждой странице.

PDF создаётся в памяти, без отправки снимков и внешних API. Фотография текущего результата хранится
только в памяти сессии; пять записей истории не содержат фотографии. В отчёт встроен свободный
DejaVu Sans с поддержкой русских и казахских букв; лицензия находится в `assets/fonts/`.
Заметки пользователя экранируются и не интерпретируются как HTML.

## Демонстрационный режим

Включается по умолчанию при отсутствии весов болезней и заметно обозначен в интерфейсе и истории. Вероятности вычисляются из SHA-256 RGB-пикселей подготовленного снимка и его размеров. Для одинакового изображения результат воспроизводим; вероятности всех четырёх классов суммируются до 1. Это не результат нейросети и не оценка состояния растения. Перекодирование, изменение ориентации или размера могут изменить пиксели и результат.

Режим полезен для проверки загрузки, результатов, порогов и истории до обучения. Нет автоматического скрытого перехода с настоящей модели на demo: если весов нет или они повреждены, приложение объясняет проблему, а режим выбирает пользователь.

## Подключение настоящей модели

1. Обучите MobileNetV3 Small по инструкции ниже.
2. Разместите доверенный файл в `model/wheat_model.pth`.
3. Выполните `python scripts/download_guard.py` для локальной проверки снимков.
4. Запустите приложение и отключите «Демонстрационный режим» в боковой панели.

Поддерживаются обычный `state_dict` и checkpoint с ключом `model_state_dict`. Выходной слой — `Linear(..., 4)`. Порядок выходов **healthy, leaf_rust, septoria, yellow_rust**: это алфавитный порядок `ImageFolder`, а не порядок перечисления болезней в тексте задания. Скрипт обучения проверяет этот контракт у всех трёх выборок. Checkpoint с несовпадающим `classes` отклоняется; порядок голого state_dict невозможно проверить по метаданным, поэтому за него отвечает автор весов.

В инференсе и val/test используются одни и те же Resize(256), CenterCrop(224), ToTensor, ImageNet mean/std из `utils/model_config.py`. Модель использует `eval()`, `no_grad()` и softmax. Архитектура при инференсе создаётся с `weights=None`, поэтому никаких скачиваний нет. Кэш учитывает размер и время изменения файла. Загрузка использует `weights_only=True`; выбирайте только доверенные файлы.

## Датасет и обучение

Публичный набор загружается с разрешённой площадки **Kaggle**:
[Wheat Disease Dataset — Small](https://www.kaggle.com/datasets/yasserhessein/wheat-disease-dataset-small).
Архив около 1 ГБ, API-ключ не требуется. Запускайте из папки проекта:

```powershell
python scripts/prepare_dataset.py
```

Команда создаёт `dataset_raw/`, `dataset/` и архив в `dataset_download/`.
Папки исходников и результата должны быть пустыми; существующие данные не перезаписываются.
Для нового разбиения задайте другие `--raw` и `--output`.
В `dataset/manifest.json` сохраняются URL Kaggle, дата загрузки, SHA-256 архива,
хеши изображений и распределение по выборкам. Автоматического перехода на другой сайт нет.
Атрибуция и история смены источника: `data/DATASET_LICENSE.md`.
Смена набора сама по себе не переобучает существующие веса — команда обучения ниже.


Подготовьте настоящие фотографии с проверенными агрономом метками и правом использования. Для каждого класса нужна достаточная представленность условий освещения, сортов и стадий заболевания. Не разделяйте фотографии одного растения или серии между выборками: это создаёт утечку данных. Test отложите до окончательной оценки. Удалите дубликаты и повреждённые файлы.

```text
dataset/
├── train/
│   ├── healthy/
│   ├── leaf_rust/
│   ├── yellow_rust/
│   └── septoria/
├── val/
│   ├── healthy/
│   ├── leaf_rust/
│   ├── yellow_rust/
│   └── septoria/
└── test/
    ├── healthy/
    ├── leaf_rust/
    ├── yellow_rust/
    └── septoria/
```

Все 12 папок должны содержать изображения. Пример начального разделения: 70/15/15 по независимым растениям или участкам с сохранением классов. Данные пользователей и датасет не включаются в Git.

```powershell
python scripts/train_model.py --data dataset --epochs 15 --batch-size 16 --lr 0.001 --patience 4 --seed 42 --output model/wheat_model.pth
```

Первый запуск бесплатно скачивает предобученные веса **ImageNet** через torchvision; нужен интернет, но не API-ключ. Эти веса сами по себе не распознают четыре болезни пшеницы. Для офлайн-обучения заранее заполните стандартный кэш torch на той же машине.

Только train получает RandomResizedCrop, отражение, небольшой поворот и изменение яркости/контраста. Val/test стабильны. Обучение использует Adam и cross entropy, выводит loss/accuracy по эпохам, сохраняет лучшую validation accuracy и делает early stopping (по умолчанию 4 эпохи без улучшений). После обучения на test оценивается **лучшая** модель. Batch size минимум 2; `num_workers=0` упрощает запуск в Windows.

Checkpoint содержит веса, классы, лучшую val accuracy, эпоху, параметры обучения, архитектуру и итоговые test loss/accuracy. Фиксированный seed улучшает воспроизводимость, но идентичность между разными GPU/версиями библиотек не гарантируется. При нехватке памяти уменьшите batch size. Обучение на CPU может занять значительно больше четырёх дней при большом датасете — заранее ограничьте размер эксперимента.

## Данные и зафиксированные результаты обучения

Для четырёх классов отобраны **838 реальных изображений** Wheat Disease Dataset — Small. Класс Mildew исключён. Перед разбиением удаляются точные RGB-дубли, близкие изображения объединяются по dHash, seed — 42.

| Класс | Train | Val | Test |
|---|---:|---:|---:|
| healthy | 84 | 19 | 19 |
| leaf_rust | 89 | 20 | 19 |
| septoria | 243 | 53 | 53 |
| yellow_rust | 167 | 36 | 36 |
| **Всего** | **583** | **128** | **127** |

Это внутреннее разделение по изображениям. Идентификаторов растений и полей нет, поэтому независимость на уровне полей не подтверждена. Test не используется для обновления весов или выбора лучшей эпохи; выбор checkpoint производится по val.

В метаданных локального checkpoint и файле [wheat_model.metrics.json](model/wheat_model.metrics.json) сохранены:

| Показатель | Значение |
|---|---:|
| Test Accuracy | 84,25% |
| Test Macro-F1 | 0,8399 |
| Validation Accuracy выбранной модели | 86,72% |
| Лучшая эпоха дообучения | 3 |
| Число test-изображений | 127 |
| Recall для бурой ржавчины | 78,95% |

SHA-256 проверенного файла `model/wheat_model.pth`:
`33a47252d7ea711b3e2c28dcc1e2400a2502f50a7002b803404217dd91adb0de`.

Это сохранённые результаты обучения классификатора, а не новая независимая оценка и не метрики всей системы с CLIP и отказами. Для бурой ржавчины в test только 19 снимков; оценка пока неточна. mAP и MAE подсчёта объектов к этой задаче классификации не применяются. После переобучения следует использовать новые метрики и SHA-256.

Первоначальное обучение использовало файлы из Zenodo. 20.09.2026 набор заново скачан с Kaggle: все 838 файлов и разбиение совпали. Подтверждение загрузки — [KAGGLE_DOWNLOAD.json](data/KAGGLE_DOWNLOAD.json). Текущая модель дообучена на копии Kaggle из исходного checkpoint; история исходного обучения сохраняется.

## Улучшение бурой ржавчины

Проведено три полных эпохи дообучения исходного checkpoint: inverse-frequency weighted cross entropy по train, Adam, learning rate 0,0001, batch size 16, seed 42. Лучший checkpoint выбран по validation Macro-F1, а не по test. Запуск с лимитом 15 эпох остановлен после третьей завершённой эпохи из-за времени CPU; четвёртая эпоха не вошла в результат.

На прежнем test recall бурой ржавчины вырос с 7/19 до 15/19 (36,84% → 78,95%), precision — 87,50% → 93,75%. Общая accuracy: 77,17% → 84,25%; Macro-F1: 0,7377 → 0,8399. Recall жёлтой ржавчины снизился с 27/36 до 26/36. Полное сравнение: [rust_improvement.json](model/rust_improvement.json).

Test уже использовался для оценки исходной модели и постановки задачи улучшения. Он не является новым независимым подтверждением; нужны новые полевые снимки. Метрики относятся к классификатору без CLIP и порогов отказа.

Повторение дообучения требует **исходного** checkpoint, SHA-256 `210afff5b6abbaf201f045b7d415f80c9f17e5e31fa1e98ccd40489ae4a2d477`. Сохраните его как `model/wheat_model_baseline.pth`; текущие улучшенные веса не подходят как идентичная стартовая точка.

```powershell
python scripts/train_model.py --data dataset --epochs 3 --batch-size 16 --lr 0.0001 --patience 5 --balanced --selection macro_f1 --init model/wheat_model_baseline.pth --output model/wheat_model_improved.pth
```

Исходный checkpoint сохранён локально в резервной копии вне репозитория. Для нового обучения от ImageNet можно использовать `--balanced --selection macro_f1` без `--init`; это будет отдельный эксперимент с другими метриками.

## Проверка комиссией на новой машине

На текущем этапе публичная ссылка на готовые веса не указана. Репозиторий содержит путь воспроизведения через обучение; без весов доступна только демонстрация интерфейса. Полное воспроизведение на чистой машине ещё должно быть подтверждено перед сдачей.

1. Скачайте репозиторий и выполните установку из раздела быстрого запуска.
2. В активированном окружении выполните последовательно:

```powershell
python scripts/prepare_dataset.py
python scripts/train_model.py --data dataset --epochs 15 --batch-size 16 --lr 0.001 --patience 4 --seed 42 --output model/wheat_model.pth
python scripts/download_guard.py
python -m pytest -q
python -m streamlit run app.py
```

3. Откройте `http://localhost:8501`, отключите демонстрационный режим. Загрузите фото из `dataset/test/healthy/` либо другого класса `dataset/test/`, нажмите «Провести диагностику».
4. Проверьте результат, время, Top-3 и рекомендации. Если CLIP отклоняет кадр, приложение должно показать отказ; это ограничение системы, его нельзя считать успешной диагностикой.
5. Переключите Русский / Қазақша, скачайте PDF, проверьте историю. Для проверки ошибок загрузите повреждённый файл изображения.

Первичная установка и скачивание данных/весов требуют интернета и времени. После подготовки локальный анализ работает без внешнего соединения. Чтобы повторить именно опубликованные метрики, комиссии потребуется исходный checkpoint с указанным SHA-256; повторное обучение может дать другой результат.

## Телефон, слабый интернет и время отклика

Подготовленный компьютер может работать сервером в локальной Wi-Fi-сети без внешнего интернета:

```powershell
python -m streamlit run app.py --server.address=0.0.0.0 --server.port=8501
ipconfig
```

На телефоне в той же сети откройте `http://IP-КОМПЬЮТЕРА:8501`, подставив IPv4 из `ipconfig`. Адрес `0.0.0.0` не предназначен для открытия в браузере. При необходимости разрешите Python входящие подключения только для доверенной частной сети. Загрузка готовой фотографии доступна по HTTP; камера браузера может требовать HTTPS.

Автономная работа модели непосредственно на телефоне не реализована. Для удалённого сервера слабый интернет влияет на загрузку фото и интерфейса. Уменьшение фотографии на сервере не сокращает уже переданный объём. Проверка удалённого сценария при ограниченной скорости сети пока не выполнена.

ТЗ требует отклик не более 5 секунд. Таймер приложения измеряет обработку на сервере после прогрева, включая CLIP, но исключает передачу фото и отрисовку. Подтверждённого полного замера на целевом устройстве при слабом интернете пока нет. Перед сдачей нужно зафиксировать оборудование, размер фото, сеть, холодный/прогретый запуск и время от действия пользователя до результата; требование пока не заявляется как полностью проверенное.

## Тесты

```powershell
python -m pytest -q
python -c "import app; print('Импорт app.py успешен')"
```

Тесты проверяют справочник, неизвестный класс, пороги, детерминизм demo, сумму вероятностей, RGB/RGBA, форматы, EXIF, повреждённые файлы, лимиты, качество, оба формата весов, порядок классов, запуск UI и очистку истории. Временные случайные веса создаются pytest исключительно для проверки загрузчика и не сохраняются в `model/`. Проверки не измеряют агрономическую точность модели.

## Docker

```powershell
docker build -t dalascan-ai .
docker run --rm -p 8501:8501 dalascan-ai
```

Образ использует Python 3.11 и CPU-сборку torch, запускается от непривилегированного пользователя. Откройте [localhost:8501](http://localhost:8501). Веса и датасет исключены из контекста сборки. Для настоящей модели смонтируйте папку с весами:

```powershell
docker run --rm -p 8501:8501 --mount "type=bind,source=$($PWD.Path)\model,target=/app/model,readonly" dalascan-ai
```

## Скорость, конфиденциальность и ограничения

- Цель — менее 5 секунд на один анализ после прогрева модели. Время измеряется `perf_counter`: проверка и декодирование файла, подготовка, качество, инференс. Загрузка файла через сеть, отрисовка и первичная загрузка/прогрев весов в него не входят. При превышении 5 секунд UI показывает предупреждение; на любом оборудовании SLA не гарантируется.
- Снимки обрабатываются в памяти сервера; приложение не пишет их на диск и не вызывает внешние API. При локальном запуске сервер — ваш компьютер. На удалённом сервере изображения передаются этому серверу, поэтому публиковать приложение следует только в доверенной среде с HTTPS.
- История привязана к Streamlit-сессии, без БД и общего журнала. Перезапуск/потеря сессии удаляют её; очистка истории не убирает текущий результат.
- Камера зависит от разрешений браузера и безопасного контекста (HTTPS/localhost).
- Добавлена экспериментальная CLIP-проверка «похоже на пшеницу». Неподходящие и сомнительные снимки не поступают в модель болезней. Это не ботанически валидированный классификатор: похожие злаки могут пройти фильтр. Сегментации листа, оценки тяжести по площади и распознавания неизвестных болезней нет.
- Softmax не является калиброванной достоверностью; пороги 45/70% — правила интерфейса, а не доказанные клинические/агрономические пороги. Даже высокая вероятность не гарантирует правильный результат.
- Риск в справочнике — общая характеристика состояния. При недостаточной уверенности риск снимка отмечается как неопределённый. Справочные признаки не являются обнаруженными моделью признаками.
- Оценка резкости/яркости — эвристика; однотонный здоровый лист может получить совет переснять фото.
- Нет авторизации, ограничения частоты запросов и инфраструктуры для массового публичного сервиса. MVP рассчитан на демонстрацию и локальную проверку.

**Результат является предварительной оценкой и не заменяет консультацию агронома. Перед применением средств защиты растений необходимо проверить местные регламенты и инструкцию производителя.**

## Ожидаемый эффект и развитие

Гипотеза пользы — сокращение времени первичного обследования и удобная передача наблюдений агроному. Для пилота в хозяйстве Акмолинской области нужно сравнить время обычного обследования и обследования с приложением, учитывать ошибочные результаты и стоимость повторной проверки.

Расчёт ожидаемой экономии труда: `число обследований × сэкономленные минуты / 60 × стоимость часа`. Значения должны быть измерены в пилоте. Экономия средств защиты, рост урожайности и снижение потерь сейчас не доказаны и не заявляются как достигнутые.

Следующие шаги: независимая полевая проверка, улучшение recall бурой ржавчины, оценка всей цепочки с CLIP, калибровка уверенности, данные разных сортов и сезонов, проверка похожих злаков, оптимизация передачи фото и развёртывание. Для других культур потребуются новые размеченные данные, обучение и отдельная проверка качества; замены названий классов недостаточно.

## Сценарий видео и очной защиты

**Видео 2–3 минуты:** проблема фермера → загрузка реального test-снимка в настоящем режиме → результат и ограничения → казахский интерфейс → PDF → метрики и дальнейший полевой пилот. Показать также отказ на неподходящем снимке. Демонстрационный режим не использовать как доказательство работы AI.

**Очная защита до 7 минут:** проблема — 1 минута; решение и живая демонстрация — 3; данные, модель и метрики — 2; эффект и развитие — 1. Вопросы — до 3 минут. На случай отсутствия интернета подготовить локальное приложение с весами и записанное видео.

## Материалы и чек-лист сдачи

По предоставленному ТЗ дедлайн формы сдачи — **21.09.2026 до 23:59**, Demo Day — 23.09.2026. Коммиты после дедлайна не учитываются. Развёрнутый сервис необязателен, если предоставлена воспроизводимая инструкция локального запуска.

- [x] Описание, трек, стек, источники, использование предобученных моделей и Codex раскрыты.
- [x] Инструкции установки, подготовки данных, обучения и запуска приведены.
- [x] Сохранённые метрики классификатора и ограничения описаны.
- [x] Состав команды указан.
- [ ] Индивидуальные роли и вклад участников уточнены.
- [ ] Доступ комиссии к актуальному репозиторию проверен.
- [ ] Настоящий сценарий воспроизведён на чистой машине без помощи разработчика.
- [ ] Предоставлен доступ к checkpoint с указанным SHA-256, если требуется повторение именно опубликованного результата.
- [ ] Проверены полный отклик до 5 секунд и сценарий слабого интернета.
- [ ] Демо-видео 2–3 минуты: ссылка пока не предоставлена.
- [ ] Презентация PDF до 10 слайдов: ссылка пока не предоставлена. Содержание: проблема, решение, демонстрация, данные и модель, метрики, экономический эффект, команда, планы.
- [ ] Капитан отправил форму с названием, треком/задачей, составом команды, репозиторием, видео, PDF и инструкцией запуска до дедлайна.

Согласно предоставленным положению и ТЗ, подача связана с условиями передачи прав и материалов организатору; команда должна ознакомиться с ними до отправки. Исходники, данные с атрибуцией, веса и материалы следует сохранить: ТЗ предусматривает предоставление запрошенных материалов в течение трёх рабочих дней. Публикация этого README сама по себе не подтверждает отправку формы или принятие проекта комиссией.

## Технические источники

[MobileNetV3 Small и преобразования](https://docs.pytorch.org/vision/main/models/generated/torchvision.models.mobilenet_v3_small.html), [Streamlit AppTest](https://docs.streamlit.io/develop/api-reference/app-testing/st.testing.v1.apptest).
