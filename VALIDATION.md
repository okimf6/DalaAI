# Результаты проверки

Дата: 18 сентября 2026 года. Среда: Windows x64.

- Python 3.11.16: **29 passed in 28.20s**.
- Дополнительная проверка Python 3.12.14: **29 passed in 8.11s**.
- Импорт `app.py` успешен на обеих версиях.
- Реальный запуск Streamlit на localhost:8501; страница проверена в браузере.
- На ширине 390 CSS px ширина документа также 390 px: горизонтального переполнения нет.
- AppTest: загрузка синтетического PNG, шесть последовательных анализов, ограничение истории пятью записями, очистка, обработка повреждённого файла, сброс устаревшего результата и отсутствие весов.
- Модель: проверены оба формата весов, несовпадение порядка классов и повреждённый checkpoint. Веса в этих тестах случайные, временные; агрономическую точность тесты не оценивают.
- Обучение: полный короткий цикл train/val/test, сохранение checkpoint и последующий инференс. В тесте отключено скачивание ImageNet; это проверка кода на синтетических изображениях, а не обучение распознавания болезней.
- Производительность demo на Python 3.11: 10 анализов JPEG 1920×1080, медиана **0,0839 с**, максимум **0,0870 с**. Включены чтение/проверка, RGB/resize, качество и прогноз. Результат нельзя переносить на любое оборудование или реальные обученные веса.
- Dockerfile создан; сборка контейнера не проверена, поскольку Docker недоступен.
- Настоящие веса и датасет не поставляются. Для реальной диагностики необходимы обучение и независимая проверка.

Повторить автоматическую проверку: `python -m pytest -q`.
